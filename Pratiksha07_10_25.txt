//Pratiksha Mahajan
//34

Q.1

package WapperClass;

public class WapperClassMain {
    public static void main(String[] args) {
        
        Integer intref = 100;
        Integer intref2 = 100;
        System.out.println("Using == : \nx == y ? "+(intref==intref2));
        System.out.println("Using equals : \nx == y ?"+intref.equals(intref2));

        Float floatref = 200.0f;
        Float floatref1 = 200.0f;

        System.out.println("Using == : \nx == y ? "+(floatref==floatref1));
        System.out.println("Using equals : \nx == y ?"+floatref.equals(floatref1));



    }

}


Q.2

    package stringBuilder;

    import java.util.Scanner;

    public class RemoveDupElement {
        public static void main(String[] args) {
            Scanner sc = new Scanner(System.in);

            String str;
            boolean found;

            System.out.println("Enter the String : ");
            str = sc.nextLine();

            int n = str.length();
            
            StringBuilder sb = new StringBuilder();

            for (int i = 0; i <n ; i++) 
            {
                found = false;
                for(int j = 0;j<sb.length();j++ )
                {
                    if(str.charAt(i)==sb.charAt(j)){
                        found = true;
                    }
                }
                if (found == false) {
                    sb.append(str.charAt(i));
                }
            }

            System.out.println(sb.toString());
        }
    }



Q.3
package stringBuilder;

import java.util.Scanner;

public class StringBuilderMain {

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
    
        System.out.println("Enter the number of element of array : ");
        int n = sc.nextInt();
        String str = sc.nextLine();

        String[] arr;
        arr = new String[n];

        System.out.println("Enter the String : ");
        StringBuilder sb = new StringBuilder();

        for (int i = 0; i < arr.length; i++) 
        {
            arr[i] = sc.nextLine();
            sb.append(arr[i]);
            sb.append(" ");
            
        }
        
        System.out.println(sb.toString());
        
    }
}


Q.4

package palindrome;

public class Palindrome {
    public static void main(String[] args) {
        String str = "MOM";
        String str2 = str.toLowerCase();
        System.out.println(str2);
        int n = str2.length();
        boolean isPalindrome = true;

        for (int i = 0, j=n-1; i < n-1 &&j>0; i++,j--) {
                if(str.charAt(i)!=str.charAt(j)){
                    isPalindrome = false;
                    break;
            }
        }
        if (isPalindrome) {
            System.out.println("Palindrome");
        }    
        else{
            System.out.println("Not Palindrome");
        }   
    }
}

